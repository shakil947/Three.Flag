import 'package:flutter/material.dart';

class WhiteSQ extends StatelessWidget {
  const WhiteSQ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      height: 255.0,
      width: 500.0,
    );
  }
}
