import 'package:flutter/material.dart';

class RedSQ extends StatelessWidget {
  const RedSQ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red[800],
      height: 100.0,
      width: 500.0,
    );
  }
}
