import 'package:flutter/material.dart';

class GreenSQ extends StatelessWidget {
  const GreenSQ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green[800],
      height: 255.0,
      width: 500.0,
    );
  }
}
